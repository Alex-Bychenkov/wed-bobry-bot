# Часто задаваемые вопросы (FAQ)

## Использование бота

### Как начать работу с ботом?

Отправьте команду `/start` в чате группы. Бот покажет кнопки для выбора статуса участия.

### Почему бот просит фамилию?

При первом использовании бот запрашивает фамилию один раз. Она будет использоваться во всех списках участников. В дальнейшем фамилия уже не запрашивается.

### Как изменить свой статус?

Отправьте `/start` еще раз и нажмите другую кнопку. Ваш статус обновится автоматически.

### Почему мои сообщения удаляются?

Бот автоматически удаляет служебные сообщения (команды, фамилии) через 3 секунды для поддержания чистоты чата. Это нормальное поведение.

### Как посмотреть список участников?

Отправьте команду `/status`. Бот покажет актуальный список всех участников, разделённых по статусам.

### Могу ли я добавить участника, которого нет в группе?

Да, но только если вы администратор. Нажмите кнопку "➕ Добавить участника не из группы" и введите фамилию.

---

## Для администраторов

### Бот не удаляет сообщения пользователей. Что делать?

**Причина:** Боту не даны права администратора в группе.

**Решение:** 
1. Откройте настройки группы → "Администраторы"
2. Добавьте бота `@Bobry_Mytishchi_Bot` в администраторы
3. Включите права "Удаление сообщений" и "Закрепление сообщений"

📖 Подробнее: [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md)

### Что удаляет бот с правами администратора и без них?

**БЕЗ прав администратора:**
- ✅ Только свои сообщения (подтверждения, запросы)
- ❌ Команды пользователей останутся в чате
- ❌ Сообщения с фамилиями останутся в чате

**С правами администратора:**
- ✅ Свои сообщения
- ✅ Команды пользователей (`/start`, `/status`, `/reset`, `/close`)
- ✅ Сообщения пользователей с фамилиями
- ✅ Чат остается чистым

### Как сбросить сессию и начать новую?

Отправьте команду `/reset`. Это создаст новую пустую сессию для следующей встречи. Все текущие ответы участников будут очищены.

### Как закрыть сессию?

Отправьте команду `/close`. Сессия завершится, и участники больше не смогут изменить свой статус.

### Кто может использовать команды /reset и /close?

Только администраторы группы или пользователи, чьи ID указаны в переменной `ADMIN_IDS` в файле `.env`.

### При вызове /start старое сообщение с кнопками не удаляется. Почему?

**Возможные причины:**
1. Боту не даны права администратора (не может удалять свои старые сообщения в некоторых случаях)
2. Старое сообщение было создано в другой сессии бота (после перезапуска)

**Решение:** Убедитесь, что бот имеет права администратора.

---

## Технические вопросы

### Какие переменные нужны в .env файле?

**Обязательные:**
- `BOT_TOKEN` — токен бота от BotFather
- `CHAT_ID` — ID группы, где работает бот
- `ADMIN_IDS` — ID администраторов через запятую

**Опциональные:**
- `TIMEZONE` — часовой пояс (по умолчанию: Europe/Moscow)
- `NOTIFY_TIME` — время уведомлений (по умолчанию: 11:00)

### Как получить CHAT_ID?

1. Добавьте бота в группу
2. Отправьте любое сообщение в группу
3. Используйте [@userinfobot](https://t.me/userinfobot) или [@getidsbot](https://t.me/getidsbot)
4. Скопируйте ID группы (начинается с `-100`)

### Как получить ADMIN_IDS?

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он пришлет ваш Telegram ID
3. Укажите ID через запятую в `ADMIN_IDS`

### Бот не запускается. Что делать?

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

2. **Проверьте файл .env:**
   - Все обязательные переменные заполнены?
   - `BOT_TOKEN` корректный?
   - `CHAT_ID` правильный (начинается с `-100`)?

3. **Проверьте, что бот добавлен в группу**

### Как обновить бота после изменений в коде?

```bash
./scripts/deploy.sh
```

Скрипт автоматически скопирует новые файлы на сервер и перезапустит бота.

### Список участников не обновляется. Почему?

**Возможные причины:**
1. Сессия закрыта (используйте `/reset` для создания новой)
2. Проблема с базой данных (проверьте логи)
3. Бот не имеет прав на редактирование сообщения

**Решение:** Проверьте логи и убедитесь, что сессия открыта.

### Когда бот автоматически напоминает о встрече?

- **Среда в 11:00** — напоминание о встрече
- **Суббота в 11:00** — напоминание о встрече
- **Среда в 23:30** — автоматическое закрытие сессии

Время можно изменить в переменной `NOTIFY_TIME` в `.env`.

---

## Мониторинг

### Как посмотреть метрики бота?

Откройте Grafana: http://87.247.157.122:3000

- Логин: `admin`
- Пароль: `admin`

Дашборд "Wednesday Bobry Bot" показывает все ключевые метрики.

### Не могу подключиться к Grafana из корпоративной сети. Что делать?

**Причина:** Корпоративный firewall может блокировать нестандартные порты (3000).

**Решение 1: SSH туннель (самое быстрое)**
```bash
./scripts/grafana-tunnel.sh
```
Или вручную:
```bash
ssh -L 3000:localhost:3000 root@87.247.157.122
```
Затем откройте в браузере: http://localhost:3000

**Решение 2: Проверить и открыть порт на сервере**
```bash
./scripts/check-grafana-access.sh
```

**Решение 3: Настроить nginx reverse proxy на стандартный HTTPS порт (443)**
См. подробную инструкцию в [GRAFANA_ACCESS.md](GRAFANA_ACCESS.md)

**Решение 4: Использовать VPN**
Если у вас есть VPN к серверу, подключитесь через него.

### Grafana не открывается / показывает ошибку

**Проверьте:**
1. Grafana запущена:
   ```bash
   ssh root@87.247.157.122 "docker ps | grep grafana"
   ```

2. Логи Grafana:
   ```bash
   ssh root@87.247.157.122 "cd /opt/monitoring && docker-compose -f docker-compose.monitoring.yml logs grafana"
   ```

3. Порт открыт в firewall:
   ```bash
   ssh root@87.247.157.122 "ufw status | grep 3000"
   ```

### Как настроить уведомления об ошибках?

Уведомления настраиваются в файле `monitoring/alertmanager.yml`. 

📊 Подробнее: [monitoring/MONITORING.md](monitoring/MONITORING.md)

### Где посмотреть логи бота?

**На сервере:**
```bash
ssh root@your-server "cd /opt/wed-bobry-bot && docker-compose logs -f"
```

**Локально:**
```bash
docker-compose logs -f
```

---

## Безопасность

### Где хранится токен бота?

В файле `.env`, который НЕ должен попадать в Git. Убедитесь, что `.env` указан в `.gitignore`.

### Можно ли использовать бота в нескольких группах?

Нет, бот настроен для работы в одной конкретной группе, указанной в `CHAT_ID`. Для работы в нескольких группах нужно запустить несколько экземпляров бота с разными `.env` файлами.

### Как защитить сервер?

1. Используйте SSH-ключи вместо паролей
2. Отключите вход по паролю в SSH
3. Настройте firewall (UFW)
4. Регулярно обновляйте систему

📖 Подробнее: [DEPLOYMENT.md](DEPLOYMENT.md)

---

## Дополнительная помощь

Если ваш вопрос не указан здесь:

1. Проверьте [README.md](README.md) для полной документации
2. Посмотрите [QUICK_START.md](QUICK_START.md) для быстрого старта
3. Изучите логи: `docker-compose logs -f`
4. Проверьте файл конфигурации `.env`
