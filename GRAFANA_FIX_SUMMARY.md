# Решение проблемы доступа к Grafana из корпоративной сети X5

## Проблема

Не удается подключиться к Grafana (http://87.247.157.122:3000) из корпоративной сети X5.

## Основная причина

**Корпоративный firewall/прокси блокирует нестандартный порт 3000.**

Большинство корпоративных сетей разрешают только стандартные порты:
- 80 (HTTP)
- 443 (HTTPS)
- 22 (SSH)
- 21 (FTP)
- и некоторые другие

Порт 3000 (Grafana) обычно блокируется для безопасности.

## Созданные решения

### 1. SSH Туннель (БЫСТРОЕ РЕШЕНИЕ) ⚡

**Использование:**
```bash
./scripts/grafana-tunnel.sh
```

Или вручную:
```bash
ssh -L 3000:localhost:3000 root@87.247.157.122
```

Затем откройте в браузере: http://localhost:3000

**Преимущества:**
- ✅ Работает сразу
- ✅ Не требует настройки сервера
- ✅ Безопасно (все через SSH)

**Недостатки:**
- ❌ Нужно держать терминал открытым
- ❌ Требуется SSH доступ

### 2. Скрипт диагностики 🔍

**Использование:**
```bash
./scripts/check-grafana-access.sh
```

**Что делает:**
- Проверяет SSH доступ
- Проверяет статус Grafana контейнера
- Проверяет firewall на сервере
- Проверяет доступность порта
- Предлагает автоматическое исправление

### 3. Nginx Reverse Proxy (РЕКОМЕНДУЕТСЯ ДЛЯ ПРОДАКШЕНА) 🔒

**Настройка:** См. подробную инструкцию в `GRAFANA_ACCESS.md`

**Преимущества:**
- ✅ Работает через стандартные порты 80/443
- ✅ Поддержка HTTPS/SSL
- ✅ Не блокируется корпоративными firewall
- ✅ Можно настроить базовую аутентификацию
- ✅ Работает из любой сети

**Требует:**
- Домен или поддомен (например, grafana.yourdomain.com)
- Настройку nginx на сервере
- SSL сертификат (Let's Encrypt - бесплатно)

## Созданные файлы

1. **`GRAFANA_ACCESS.md`**
   - Полная документация по проблеме
   - 5 возможных причин и их решения
   - Пошаговая инструкция настройки nginx
   - Диагностика проблем

2. **`scripts/check-grafana-access.sh`**
   - Автоматическая диагностика
   - Проверка всех компонентов
   - Предложения по исправлению

3. **`scripts/grafana-tunnel.sh`**
   - Быстрое создание SSH туннеля
   - Автоматическое открытие браузера
   - Простое использование

4. **Обновленная документация:**
   - `README.md` - добавлена секция о проблемах доступа
   - `QUICK_START.md` - добавлены быстрые решения
   - `FAQ.md` - добавлен раздел "Не могу подключиться к Grafana"
   - `monitoring/MONITORING.md` - добавлена секция о проблемах доступа

## Рекомендации

### Для немедленного использования (СЕЙЧАС):

```bash
# 1. Запустите SSH туннель
./scripts/grafana-tunnel.sh

# 2. Откройте в браузере
http://localhost:3000

# 3. Логин: admin, пароль: admin
```

### Для долгосрочного решения (ПОЗЖЕ):

1. Получите домен (например, `grafana.yourdomain.com`)
2. Настройте DNS (A-запись → 87.247.157.122)
3. Настройте nginx reverse proxy (см. `GRAFANA_ACCESS.md`)
4. Получите SSL сертификат (Let's Encrypt)
5. Доступ из любой сети: https://grafana.yourdomain.com

## Чеклист для проверки

На сервере:
- [ ] Grafana контейнер запущен: `docker ps | grep grafana`
- [ ] Порт 3000 слушает: `ss -tlnp | grep 3000`
- [ ] Firewall разрешает порт 3000: `ufw allow 3000/tcp`

С вашего компьютера:
- [ ] Сервер доступен: `ping 87.247.157.122`
- [ ] SSH работает: `ssh root@87.247.157.122`
- [ ] SSH туннель создан: `./scripts/grafana-tunnel.sh`
- [ ] Grafana открывается: http://localhost:3000

## Дополнительные варианты

### Вариант 4: VPN
Если есть VPN к серверу - подключайтесь через него.

### Вариант 5: Открыть порт в firewall сервера
```bash
ssh root@87.247.157.122
ufw allow 3000/tcp
```

**Внимание:** Это не поможет, если корпоративная сеть блокирует порт 3000 на своей стороне.

## Помощь

Если ничего не помогает:

1. **Проверьте логи Grafana:**
   ```bash
   ssh root@87.247.157.122 "cd /opt/monitoring && docker-compose -f docker-compose.monitoring.yml logs grafana"
   ```

2. **Свяжитесь с сетевым администратором X5:**
   - Узнайте, какие порты разрешены
   - Попросите разблокировать порт 3000 для IP 87.247.157.122
   - Или используйте VPN

3. **Используйте SSH туннель как временное решение:**
   ```bash
   ./scripts/grafana-tunnel.sh
   ```

## Итог

✅ **Созданы инструменты для решения проблемы**
✅ **Добавлена документация**
✅ **Готовы быстрые и долгосрочные решения**

**Используйте SSH туннель прямо сейчас:**
```bash
./scripts/grafana-tunnel.sh
```
