# Быстрый старт - Развертывание и использование бота

## 📋 Содержание

1. [Как пользоваться ботом (для участников)](#как-пользоваться-ботом)
2. [Как пользоваться ботом (для администраторов)](#для-администраторов)
3. [Развертывание на сервере](#развертывание-на-сервере)

---

## Как пользоваться ботом

### Для участников

#### Отметить свое участие

1. **Отправьте команду `/start` в чате**
   ```
   /start
   ```
   или с упоминанием бота:
   ```
   /start@Bobry_Mytishchi_Bot
   ```

2. **Нажмите кнопку с вашим статусом:**
   - 🟢 **"Я буду, запиши меня"** — точно придёте
   - 🟡 **"Пока не определился"** — ещё думаете
   - 🔴 **"Не смогу пойти, занят"** — не сможете прийти

3. **Если это первый раз:**
   - Бот попросит указать фамилию
   - Введите фамилию (например: `Иванов`)
   - Фамилия сохранится и будет использоваться в списках

4. **Готово!** Ваш статус обновлён в общем списке

#### Посмотреть список участников

**Отправьте команду `/status`:**
```
/status
```

Бот покажет список всех участников, разделённых по статусам.

#### Изменить своё решение

1. Отправьте `/start` еще раз
2. Нажмите другую кнопку
3. Статус обновится автоматически

> 💡 **Совет:** Все служебные сообщения (команды, фамилии) автоматически удаляются через 3 секунды для чистоты чата

---

### Для администраторов

#### ⚠️ Первое, что нужно сделать

**Дайте боту права администратора в группе!**

1. Откройте настройки группы → "Администраторы"
2. Добавьте бота `@Bobry_Mytishchi_Bot` в администраторы
3. Включите права:
   - ✅ **Удаление сообщений**
   - ✅ **Закрепление сообщений**

📖 Подробная инструкция: [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md)

💬 **Совет:** Закрепите инструкцию в чате для участников! См. [CHAT_INSTRUCTIONS.md](CHAT_INSTRUCTIONS.md)

#### Команды управления

**Сбросить сессию (создать новую):**
```
/reset
```
Используйте, когда нужно начать сбор участников для следующей встречи.

**Закрыть сессию (завершить сбор):**
```
/close
```
Используйте после окончания встречи.

#### Добавить гостя (не из группы)

1. Отправьте `/start`
2. Нажмите кнопку **"➕ Добавить участника не из группы"**
3. Введите фамилию гостя
4. Гость добавится в список "Я буду"

---

## Развертывание на сервере

## Шаг 1: Подготовка файла .env

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните следующие значения:

```env
BOT_TOKEN=ваш_токен_от_BotFather
CHAT_ID=-1001234567890  # ID вашего чата
ADMIN_IDS=123456789,987654321  # ID администраторов через запятую
TIMEZONE=Europe/Moscow
NOTIFY_TIME=10:00
```

**Как получить значения:**
- `BOT_TOKEN`: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- `CHAT_ID`: Добавьте бота в группу, затем отправьте сообщение. Используйте [@userinfobot](https://t.me/userinfobot) или [@getidsbot](https://t.me/getidsbot) для получения ID
- `ADMIN_IDS`: Ваш Telegram ID и ID других администраторов (через запятую)

## Шаг 2: Развертывание

Запустите скрипт развертывания:

```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

Скрипт автоматически:
1. ✅ Проверит настройку сервера (установит Docker если нужно)
2. ✅ Скопирует файлы проекта на сервер
3. ✅ Запустит бота в Docker контейнере

## Шаг 3: Проверка работы

Проверьте статус бота:

```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose ps"
```

Просмотрите логи:

```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose logs -f"
```

## Готово! 🎉

Бот должен работать 24/7 на вашем сервере. Бот автоматически перезапускается при перезагрузке сервера.

## Полезные команды

### Просмотр логов в реальном времени
```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose logs -f"
```

### Перезапуск бота
```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose restart"
```

### Обновление бота после изменений
```bash
./scripts/deploy.sh
```

### Остановка бота
```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose down"
```

### Запуск бота
```bash
ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose up -d"
```

---

## 🔍 Мониторинг

Система мониторинга доступна по адресам:

- **Prometheus:** http://87.247.157.122:9090
- **Grafana:** http://87.247.157.122:3000 (логин: `admin`, пароль: `admin`)
- **Alertmanager:** http://87.247.157.122:9093

### Не можете подключиться к Grafana из корпоративной сети?

**Решение 1: SSH туннель (быстро)**
```bash
./scripts/grafana-tunnel.sh
```
Затем откройте: http://localhost:3000

**Решение 2: Диагностика**
```bash
./scripts/check-grafana-access.sh
```

**Решение 3: Nginx reverse proxy**
См. подробную инструкцию в [GRAFANA_ACCESS.md](GRAFANA_ACCESS.md)

📊 Подробнее: [monitoring/MONITORING.md](monitoring/MONITORING.md)

## 🔧 Устранение неполадок

### Бот не запускается

1. **Проверьте логи:**
   ```bash
   ssh root@77.110.105.104 "cd /opt/wed-bobry-bot && docker-compose logs"
   ```

2. **Проверьте файл .env:**
   ```bash
   ssh root@77.110.105.104 "cat /opt/wed-bobry-bot/.env"
   ```

3. **Убедитесь, что все переменные заполнены правильно**

### Бот не удаляет сообщения

**Причина:** Боту не даны права администратора в группе

**Решение:** Добавьте бота в администраторы группы с правами:
- ✅ Удаление сообщений
- ✅ Закрепление сообщений

📖 См. [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md)

### Бот не отвечает на команды

1. **Проверьте, что бот добавлен в группу**
2. **Проверьте, что CHAT_ID в .env соответствует ID группы**
3. **Проверьте логи на наличие ошибок**

### Список участников не обновляется

1. **Проверьте, что сессия не закрыта** (используйте `/reset` для создания новой)
2. **Проверьте логи на наличие ошибок с базой данных**

## 🔒 Безопасность

⚠️ **Важно:** После успешного развертывания настоятельно рекомендуется:

### 1. Настроить SSH-ключ для входа без пароля

```bash
ssh-copy-id root@77.110.105.104
```

### 2. Отключить вход по паролю

Отредактируйте `/etc/ssh/sshd_config` на сервере:
```bash
PasswordAuthentication no
```

Перезапустите SSH:
```bash
systemctl restart sshd
```

### 3. Изменить пароль root

```bash
passwd
```

### 4. Не публиковать .env файл

Файл `.env` содержит конфиденциальные данные (токен бота). Никогда не добавляйте его в Git!

---

## 📚 Дополнительные ресурсы

- [README.md](README.md) — полная документация проекта
- [DEPLOYMENT.md](DEPLOYMENT.md) — подробное руководство по развертыванию
- [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md) — настройка прав администратора
- [AUTO_DELETE_BEHAVIOR.md](AUTO_DELETE_BEHAVIOR.md) — поведение автоудаления сообщений
- [monitoring/MONITORING.md](monitoring/MONITORING.md) — руководство по мониторингу

---

## 💬 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что бот имеет права администратора
3. Проверьте файл `.env` на корректность данных
4. Обратитесь к документации в соответствующих файлах
