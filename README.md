# Бот Бобры - Telegram бот для управления списками участников

Telegram бот для управления списками участников встреч по средам. Бот позволяет участникам отмечать свое участие через интерактивные кнопки.

## Возможности

- ✅ Отметка участия через интерактивные кнопки (Буду / Не определился / Не смогу)
- 📋 Автоматическое формирование и обновление списков участников
- 🔔 Напоминания о встречах (среда и суббота в 11:00)
- 👥 Управление сессиями администраторами
- ➕ Добавление гостей (участников не из группы) администраторами
- 🧹 Автоматическое удаление служебных сообщений для чистоты чата
- 💾 Хранение данных в SQLite с WAL режимом для высокой производительности
- 📊 Интеграция с Prometheus и Grafana для мониторинга
- 🔒 Разграничение прав доступа (администраторы / участники)

## Как пользоваться ботом

### Для участников

#### 1. Начало работы

**Отправьте команду `/start` в чате:**
```
/start
```
или используйте упоминание бота:
```
/start@Bobry_Mytishchi_Bot
```

Бот покажет сообщение с кнопками для выбора статуса участия.

> 💡 **Совет:** Команда `/start` автоматически удалится через 3 секунды для чистоты чата

#### 2. Выбор статуса участия

Нажмите одну из кнопок:

- 🟢 **"Я буду, запиши меня"** — вы точно придёте на встречу
- 🟡 **"Пока не определился"** — ещё думаете, пойдёте или нет
- 🔴 **"Не смогу пойти, занят"** — не сможете прийти

Ваш статус мгновенно обновится в общем списке участников.

#### 3. Первое использование

При первом использовании бота вам нужно указать фамилию:

1. Нажмите кнопку выбора статуса
2. Бот попросит: *"Пожалуйста, отправь свою фамилию"*
3. Отправьте вашу фамилию (например: `Иванов`)
4. Бот сохранит фамилию и обновит ваш статус
5. Ваше сообщение с фамилией автоматически удалится через 3 секунды

> 📝 **Примечание:** Фамилия запрашивается только один раз и сохраняется для всех будущих встреч

#### 4. Изменение решения

Чтобы изменить свой статус:

1. Отправьте команду `/start` еще раз
2. Нажмите другую кнопку
3. Ваш статус обновится автоматически

> ⚡ **Важно:** Вы можете менять статус сколько угодно раз до закрытия сессии

#### 5. Просмотр списка участников

**Отправьте команду `/status`:**
```
/status
```

Бот создаст новое сообщение со списком всех участников, разделённых по статусам:
- ✅ Будут (YES)
- ❓ Не определились (MAYBE)
- ❌ Не придут (NO)

> 🔄 **Обновление:** Список автоматически обновляется при каждом изменении статуса

### Для администраторов

#### ⚠️ ВАЖНО: Настройка прав администратора

**Бот ДОЛЖЕН иметь права администратора в группе для корректной работы!**

**Необходимые права:**
- ✅ **Удаление сообщений** — для автоматической очистки служебных сообщений
- ✅ **Закрепление сообщений** — для закрепления списка участников

**Без прав администратора:**
- ❌ Команды пользователей не будут удаляться
- ❌ Сообщения с фамилиями останутся в чате
- ❌ Чат будет захламлен служебными сообщениями

📖 **Подробная инструкция:** [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md)

#### Команды управления сессией

**`/reset` — Сбросить текущую сессию**
```
/reset
```
- Создаёт новую пустую сессию для следующей встречи
- Все текущие ответы участников будут очищены
- Предыдущая сессия сохраняется в базе данных
- Команда автоматически удалится через 3 секунды

**`/close` — Закрыть сессию**
```
/close
```
- Завершает сбор участников
- Участники больше не смогут изменить свой статус
- Используйте после окончания встречи
- Команда автоматически удалится через 3 секунды

> 🔒 **Доступ:** Только администраторы группы или пользователи из `ADMIN_IDS` могут использовать эти команды

#### Добавление гостей (участников не из группы)

Администраторы могут добавлять участников, которых нет в Telegram-группе:

1. Отправьте команду `/start`
2. Нажмите кнопку **"➕ Добавить участника не из группы"**
3. Введите фамилию участника (например: `Петров`)
4. Участник автоматически добавится в список "Я буду" (YES)
5. Сообщение с фамилией автоматически удалится через 3 секунды

> 👥 **Применение:** Используйте для добавления друзей, коллег или знакомых, которые придут с вами, но не состоят в группе

### Автоматические уведомления и очистка чата

#### Напоминания о встречах

- **Среда и суббота в 11:00** — бот отправляет напоминание о встрече с кнопками выбора статуса
- **Среда в 23:30** — бот автоматически закрывает сессию (после встречи)

#### Автоматическое удаление сообщений

Бот автоматически удаляет служебные сообщения для поддержания чистоты чата:

| Тип сообщения | Время удаления |
|---------------|----------------|
| Команды `/start`, `/status`, `/reset`, `/close` | 3 секунды |
| Сообщения пользователей с фамилиями | 3 секунды |
| Подтверждения ("Сессия сброшена", "Участник добавлен") | 3 секунды |
| Запросы фамилии | 15 секунд |
| Сообщение с кнопками выбора статуса | При следующем вызове `/start` |

📖 **Подробнее:** [AUTO_DELETE_BEHAVIOR.md](AUTO_DELETE_BEHAVIOR.md)

## Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```
4. Заполните необходимые переменные в `.env`
5. Запустите бота:
   ```bash
   python bot.py
   ```

### Развертывание на сервере

Подробные инструкции по развертыванию см. в [DEPLOYMENT.md](DEPLOYMENT.md)

**Быстрый способ:**

1. Подготовьте файл `.env` с настройками бота
2. Запустите скрипт развертывания:
   ```bash
   ./scripts/deploy.sh
   ```

Скрипт автоматически:
- Настроит сервер (установит Docker, Docker Compose)
- Скопирует файлы проекта
- Запустит бота в Docker контейнере

## Структура проекта

```
wed_bobry_bot/
├── bot.py              # Главный файл бота
├── handlers.py         # Обработчики команд и callback'ов
├── db.py               # Работа с базой данных
├── config.py           # Конфигурация
├── scheduler.py        # Планировщик уведомлений
├── utils.py            # Вспомогательные функции
├── Dockerfile          # Образ для Docker
├── docker-compose.yml  # Конфигурация Docker Compose
├── requirements.txt    # Python зависимости
└── scripts/            # Скрипты для развертывания
    ├── deploy.sh       # Скрипт развертывания
    ├── remote-setup.sh # Настройка сервера
    └── wed-bobry-bot.service # Systemd service
```

## Переменные окружения

Создайте файл `.env` со следующими переменными:

- `BOT_TOKEN` - токен Telegram бота (обязательно)
- `CHAT_ID` - ID чата, где работает бот (обязательно)
- `ADMIN_IDS` - ID администраторов через запятую (обязательно)
- `TIMEZONE` - часовой пояс (по умолчанию: Europe/Moscow)
- `NOTIFY_TIME` - время уведомлений (по умолчанию: 11:00)

## Команды бота

| Команда | Описание | Доступ | Удаление |
|---------|----------|--------|----------|
| `/start` | Показать кнопки выбора статуса | Все участники | 3 сек |
| `/status` | Показать текущий список участников | Все участники | 3 сек |
| `/reset` | Сбросить сессию и создать новую | Только администраторы | 3 сек |
| `/close` | Закрыть текущую сессию | Только администраторы | 3 сек |

> 💡 **Совет:** Можно использовать команды с упоминанием бота: `/start@Bobry_Mytishchi_Bot`

## Развертывание

### Требования

- Сервер с Ubuntu/Debian/CentOS
- Доступ по SSH
- Минимум 512MB RAM
- 1GB свободного места на диске

### Автоматическое развертывание

Используйте скрипт `scripts/deploy.sh` для автоматического развертывания:

```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

### Ручное развертывание

1. Настройте сервер:
   ```bash
   ssh root@your-server
   # Выполните скрипт scripts/remote-setup.sh на сервере
   ```

2. Скопируйте файлы проекта на сервер

3. Создайте `.env` файл с настройками

4. Запустите бота:
   ```bash
   cd /opt/wed-bobry-bot
   docker-compose up -d
   ```

Подробнее см. [DEPLOYMENT.md](DEPLOYMENT.md)

## Мониторинг

Система мониторинга включает Prometheus, Grafana, Alertmanager и Telegram уведомления.

**Быстрый доступ:**
- Prometheus: http://87.247.157.122:9090
- Grafana: http://87.247.157.122:3000 (admin/admin)
- Alertmanager: http://87.247.157.122:9093

### Проблемы с доступом к Grafana из корпоративной сети?

Если вы не можете подключиться к Grafana из корпоративной сети:

**Быстрое решение - SSH туннель:**
```bash
./scripts/grafana-tunnel.sh
```
Затем откройте http://localhost:3000

**Диагностика проблемы:**
```bash
./scripts/check-grafana-access.sh
```

📖 **Подробные инструкции:** [GRAFANA_ACCESS.md](GRAFANA_ACCESS.md)

**Подробная инструкция по использованию:** [monitoring/MONITORING.md](monitoring/MONITORING.md)

## Управление ботом на сервере

### Просмотр логов
```bash
ssh root@your-server "cd /opt/wed-bobry-bot && docker-compose logs -f"
```

### Перезапуск
```bash
ssh root@your-server "cd /opt/wed-bobry-bot && docker-compose restart"
```

### Остановка
```bash
ssh root@your-server "cd /opt/wed-bobry-bot && docker-compose down"
```

### Обновление
```bash
./scripts/deploy.sh
```

## Разработка

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск в режиме разработки
```bash
python bot.py
```

### Запуск через Docker
```bash
docker-compose up
```

## Часто задаваемые вопросы

Ответы на часто задаваемые вопросы см. в [FAQ.md](FAQ.md)

**Основные вопросы:**
- Как начать работу с ботом?
- Почему бот не удаляет сообщения?
- Как добавить участника не из группы?
- Как получить CHAT_ID и ADMIN_IDS?
- Как обновить бота после изменений?

## Дополнительная документация

- 📖 [QUICK_START.md](QUICK_START.md) — быстрый старт для пользователей и развертывание
- 🚀 [DEPLOYMENT.md](DEPLOYMENT.md) — подробное руководство по развертыванию
- 🔧 [BOT_ADMIN_SETUP.md](BOT_ADMIN_SETUP.md) — настройка прав администратора
- 🧹 [AUTO_DELETE_BEHAVIOR.md](AUTO_DELETE_BEHAVIOR.md) — поведение автоудаления сообщений
- 📊 [monitoring/MONITORING.md](monitoring/MONITORING.md) — руководство по мониторингу
- 🌐 [GRAFANA_ACCESS.md](GRAFANA_ACCESS.md) — решение проблем доступа к Grafana
- ❓ [FAQ.md](FAQ.md) — часто задаваемые вопросы

## Лицензия

Проект создан для внутреннего использования.
