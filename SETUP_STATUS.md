# Статус настройки сервера

## ✅ Что уже сделано

1. **Созданы скрипты для развертывания:**
   - `scripts/remote-setup.sh` - настройка сервера (установка Docker, Docker Compose)
   - `scripts/deploy.sh` - автоматическое развертывание бота
   - `scripts/wed-bobry-bot.service` - systemd service для автозапуска

2. **Создана документация:**
   - `DEPLOYMENT.md` - подробная инструкция по развертыванию
   - `QUICK_START.md` - быстрый старт
   - `README.md` - общая документация проекта

3. **Запущена настройка сервера:**
   - Скрипт настройки был запущен на сервере 77.110.105.14
   - Процесс обновления системы и установки Docker может занять 10-20 минут

## ⏳ Текущий статус

Сервер находится в процессе настройки. Система обновляется, после чего будет установлен Docker и Docker Compose.

## 📋 Следующие шаги

### Вариант 1: Дождаться завершения настройки и развернуть автоматически

Подождите 10-20 минут, затем выполните:

```bash
./scripts/deploy.sh
```

Скрипт автоматически:
- Проверит, установлен ли Docker
- Если нет - завершит настройку
- Скопирует файлы проекта
- Запустит бота

### Вариант 2: Проверить статус настройки вручную

Подключитесь к серверу и проверьте статус:

```bash
ssh root@77.110.105.14
```

Проверьте:
```bash
# Проверка Docker
docker --version

# Проверка Docker Compose
docker-compose --version

# Если не установлены, выполните настройку:
cd /opt/wed-bobry-bot
bash scripts/remote-setup.sh
```

### Вариант 3: Развернуть вручную после завершения настройки

После того как Docker установлен, выполните:

```bash
# 1. Скопируйте файлы на сервер
scp -r * root@77.110.105.14:/opt/wed-bobry-bot/

# 2. Подключитесь к серверу
ssh root@77.110.105.14

# 3. Перейдите в директорию проекта
cd /opt/wed-bobry-bot

# 4. Убедитесь, что .env файл настроен
nano .env

# 5. Запустите бота
docker-compose up -d

# 6. Проверьте логи
docker-compose logs -f
```

## 🔍 Проверка готовности сервера

Выполните команду для проверки:

```bash
sshpass -p 'UU2qWYY1TdGk' ssh root@77.110.105.14 "docker --version && docker-compose --version && echo 'Сервер готов!'"
```

Если команда выполняется успешно - сервер готов к развертыванию.

## 🚀 Быстрое развертывание (когда сервер готов)

```bash
# Убедитесь, что .env файл заполнен
cat .env

# Запустите развертывание
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

## 📞 Полезные команды после развертывания

### Просмотр логов
```bash
ssh root@77.110.105.14 "cd /opt/wed-bobry-bot && docker-compose logs -f"
```

### Статус контейнеров
```bash
ssh root@77.110.105.14 "cd /opt/wed-bobry-bot && docker-compose ps"
```

### Перезапуск
```bash
ssh root@77.110.105.14 "cd /opt/wed-bobry-bot && docker-compose restart"
```

## ⚠️ Важно

После успешного развертывания:

1. **Настройте SSH-ключ** для безопасного доступа:
   ```bash
   ssh-copy-id root@77.110.105.14
   ```

2. **Отключите вход по паролю** в `/etc/ssh/sshd_config`

3. **Измените пароль root** на более сложный

## 📚 Дополнительная информация

- Подробная инструкция: [DEPLOYMENT.md](DEPLOYMENT.md)
- Быстрый старт: [QUICK_START.md](QUICK_START.md)
- Общая документация: [README.md](README.md)
