# Все версии закреплены ТОЧНО — сборка образа должна быть воспроизводимой.
#
# Почему: 2026-08-30 пересборка с незакреплёнными версиями подтянула aiogram 3.31,
# у которого один только import стоит +133 МБ RSS (стек целиком 171 МБ против
# 114 МБ у 3.27) — бот упёрся в лимит памяти контейнера. Скачок внутри aiogram
# между 3.27 и 3.31, pydantic и базовый образ ни при чём (проверено замерами).
#
# Как обновлять: поднять версию в чистом контейнере python:3.11-alpine, снять
# `pip freeze`, замерить RSS после импортов (resource.getrusage) и только потом
# переносить сюда. Ожидаемый RSS бота в работе ~120 МБ при aiogram 3.27.

# --- прямые зависимости ---
aiogram==3.27.0
aiohttp_socks==0.12.0
aiosqlite==0.22.1
APScheduler==3.11.3
python-dotenv==1.2.3
prometheus_client==0.26.0

# --- транзитивные (pip freeze, python:3.11-alpine, 2026-08-30) ---
aiofiles==25.1.0
aiohappyeyeballs==2.7.1
aiohttp==3.13.5
aiosignal==1.4.0
annotated-types==0.8.0
attrs==26.1.0
certifi==2026.7.22
frozenlist==1.8.0
idna==3.19
magic-filter==1.0.12
multidict==6.7.1
packaging==26.3
propcache==0.5.2
pydantic==2.12.5
pydantic_core==2.41.5
python-socks==3.0.0
typing-inspection==0.4.4
typing_extensions==4.16.0
tzlocal==5.4.4
yarl==1.24.5
